---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  - type: rpm-ostree
    install:
      - kernel-devel
      - gcc
      - make
      - patch
      - curl
      - xz

  - type: script
    scripts:
      - build-mac80211.sh

  - type: rpm-ostree
    remove:
      - kernel-devel
      - gcc
      - make
